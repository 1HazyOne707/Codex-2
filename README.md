# Codex • Vectorect Starter

Deployed with **Cloudflare Pages** (no local Wrangler).  
- Static site at `/`
- Pages Function at `/api/hello` using `/functions` directory

Push to `main`, connect the repo in Cloudflare Pages, and ship.
